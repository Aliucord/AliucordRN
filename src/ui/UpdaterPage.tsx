import { React, ReactNative as RN } from "../metro";
import { styles } from "./AliucordPage";

export default function UpdaterPage() {
    return (
        <RN.Text style={styles.text}>We do a little updating</ RN.Text>
    );
}
